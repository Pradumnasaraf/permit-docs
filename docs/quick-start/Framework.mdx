

- Show Installation of Permit CLI and hyperlinking to docs. After the instlation type permit to confirm the instlation. For authorization we need to login execute permit login. This will open a browser window and ask you to login with your Permit account. After logging in, the CLI will be authenticated and ready to use. The will login into default environment.

- Permit CLi offer template policy so thatw e don't need to create from scratch. We can chcek list of policy by extecuting `permit env template list` and then apply via `permit env template apply <template-name>`.

